# pyproject.toml
[tool.pytest.ini_options]
minversion = "6.0"
addopts = [
    "-s",
    "--cov-report=html",
    "--cov=app",
    "--cov-config=.coveragerc",
    "--color=yes",
]
testpaths = [
    "testing"
]
